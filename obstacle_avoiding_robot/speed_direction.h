#ifndef SPEED_DIRECTION_H_
#define SPEED_DIRECTION_H_

constexpr unsigned int FORWARD  = 0;
constexpr unsigned int BACKWARD = 1;

enum SpeedDirection {
	CLOCKWISE        = FORWARD,
	COUNTERCLOCKWISE = BACKWARD,
};

#endif // speed_direction.h
